Cycle v1.1: 
    + Support flags for custom delimiters when parsing input CSV documents (e.g. regress.exe "x.csv" "y.csv" --delimiter ".")
    + Support flags for outputing data and regression line as a .png line chart (e.g. regress.exe "x.csv" "y.csv" lin --out "Title")
    + Return output as full equation with labels for variables:
        e.g. Output of Linear Regression
        m = 2.2
        b = 1.3
        In the form (y = mx + b)...
        y = 2.2x + 1.3
    - Remove sample dataset
    +/- Update documentation 